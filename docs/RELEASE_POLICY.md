# Release Policy

IACK releases must pass automated CI before promotion. A release is eligible only when tests pass, package build succeeds, distribution metadata validates, and the README and changelog reflect the shipped behavior. Until IACK is explicitly designated as a public package, releases are treated as controlled internal framework releases, tagged with Semantic Versioning and documented in the repository before any external publication is considered.
